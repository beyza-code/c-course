#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// Strlen Kullanımı

int main() {
  char beyza[] = "July";
  printf("The length of the character sequence:%d\n", strlen(beyza));
  printf("the length of the character sequence:%d\n", sizeof(beyza));

  return 0;
}